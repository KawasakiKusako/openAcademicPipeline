# Electron binary download mirror (faster in China / CN networks).
# Note: npm 11+ cannot pass this key through to Electron's installer (see
# scripts/ensure-electron-binary.mjs, which sets ELECTRON_MIRROR explicitly).
# Remove this file if you prefer the official GitHub releases.
electron_mirror=https://npmmirror.com/mirrors/electron/
